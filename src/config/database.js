const dataBaseConfig = {
  dialect: 'postgres',
  host: 'localhost',
  username: 'postgres',
  password: '8100',
  database: 'db_contry_list',
  define: {
    timestamps: true,
    
  }
}

module.exports = dataBaseConfig